import path from 'path';

const Map_Directory_Path = path.join(process.cwd(), 'maps');
const Model_Directory_Path = path.join(process.cwd(), 'models');
const Match_Directory_Path = path.join(process.cwd(), 'matches');
const Layout_Editor_Path = path.join(process.cwd(), './models/layoutEditor.jpg');

export { Map_Directory_Path, Model_Directory_Path, Match_Directory_Path, Layout_Editor_Path };
